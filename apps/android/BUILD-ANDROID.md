# Forge Game Center — Android APK

Abra este projeto no Android Studio e gere `app-release.apk` em **Build > Generate Signed Bundle / APK > APK**.

O aplicativo abre a Central oficial e os jogos continuam sendo executados no navegador.
